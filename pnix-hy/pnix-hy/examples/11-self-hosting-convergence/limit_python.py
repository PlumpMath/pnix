"""plain의 한계 — '자기 언어로 쓰인 런타임'과의 수렴을 증명할 수 없다.

Python으로 식을 평가하면 값 하나가 나올 뿐이다. "이 값이, 같은 언어로 '자기 자신을 구현한'
또 다른 런타임에서도 같게 나온다"는 자기호스팅(self-hosting) 수렴을 표준으로 보여주지 못한다
(CPython 자체가 C로 구현돼 있고, 교차검증할 '언어-내 구현'이 딸려오지 않는다).
"""
value = eval("2 * 3 + 4")
print("Python 평가:", value)  # 10
print("교차검증할 '자기 언어로 쓰인 두 번째 구현'?: 없음")
print("host 해석기 vs 자기호스팅 컴파일러의 '수렴 증명'?: 없음")

print("\n결론: 한 값만 나온다. 같은 언어로 자기를 구현한 런타임과의 수렴은 증명되지 않는다.")

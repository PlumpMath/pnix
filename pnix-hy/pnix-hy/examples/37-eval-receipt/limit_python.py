"""한계: 재현성/감사 영수증(소스해시+값해시+수렴+실행흔적)을 하나의 문서로
묶는 표준이 plain Python에 없다.

`eval()`은 값 하나를 돌려줄 뿐이다. "이 계산을 왜 믿어도 되는가"를 감사하려면
소스 해시, 값 해시, 여러 실행 경로의 수렴 여부, 실행 흔적을 각각 따로
챙겨야 한다 — 재검토 가능한 단일 문서가 없다.
"""
import os, sys
sys.path.insert(0, os.path.abspath(os.path.join(os.path.dirname(__file__), "..", "..")))

print("plain Python: 재현성/감사 영수증(해시+값+수렴+흔적 한 장)이 없음.")
print("한계: 계산을 감사하려면 로그를 뒤지거나 직접 조립해야 한다.")

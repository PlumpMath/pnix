{
  sub = builtins.substring 1 2 "abcd";
  join = builtins.concatStringsSep "," ["a" "b"];
  ver = builtins.splitVersion "1.2.3";
  ts = builtins.toString 42;
  has = builtins.hasAttr "a" { a = 1; };
}

# 11 — 리스트 고차 builtins

## 무엇을

`map` · `filter` · `genList` · `concatLists` · `head` 등 리스트 seed.

## 실행

```bash
cd pnix-clr
./bin/pnix-clr -e 'builtins.map (x: x + 1) [1 2 3]'
./bin/pnix-clr pnix-clr/examples/11-list-higher-order/sample.px
```

{
  mapped = builtins.map (x: x + 1) [1 2 3];
  filtered = builtins.filter (x: x > 1) [1 2 3];
  gen = builtins.genList (i: i * 2) 3;
  flat = builtins.concatLists [[1] [2 3]];
  head = builtins.head [9 8];
}

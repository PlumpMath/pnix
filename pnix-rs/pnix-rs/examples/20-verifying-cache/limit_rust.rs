//! plain Rust의 한계 — 증분 빌드 캐시가 신뢰 기반이라 변조를 탐지하지 않는다.

fn main() {
    // cargo의 증분 캐시는 hit이 나면 그대로 재사용할 뿐, 값을 다시 유도해
    // 저장된 값과 일치하는지 재검증하지 않는다.
    println!("plain Rust: 캐시 hit을 재검증(감사)하지 않는다 — 신뢰만 한다");
}

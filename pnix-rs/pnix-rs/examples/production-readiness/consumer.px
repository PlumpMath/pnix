let
  lib = import ./library.px;
  summary = lib.summarize [ 1 2 3 4 ];
in {
  answer = lib.double 21;
  mapped = lib.mapDouble [ 1 2 3 ];
  count = summary.count;
  total = summary.total;
  version = lib.version;
}

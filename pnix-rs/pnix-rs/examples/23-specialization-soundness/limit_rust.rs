//! plain Rust의 한계 — 특화가 어떤 정적 가정에 의존하는지 추적하지 않고,
//! 가정이 깨졌을 때 재사용 안전성을 검사하지 않는다.

fn main() {
    // 상수접기는 값을 접을 뿐, "이 결과가 유효한 가정 집합"과 "그 가정이
    // 바뀌면 stale이 된다"는 것을 구조적으로 추적하지 않는다.
    println!("plain Rust: 특화의 phase 경계/가정 유효성 추적이 없다");
}

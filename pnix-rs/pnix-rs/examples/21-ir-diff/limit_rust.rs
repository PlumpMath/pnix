//! plain Rust의 한계 — 정본 IR 레벨에서 "어디서부터 의미가 갈라지는가"를
//! 짚어주는 구조적 diff가 없다(텍스트 diff만 있다).

fn main() {
    // git diff는 라인 단위 텍스트 비교다. 재배열이 의미보존인지, 실제
    // 의미 변경의 첫 위치가 어디인지는 알려주지 않는다.
    println!("plain Rust: 정본 IR 구조 diff(첫 차이 위치)가 없다");
}

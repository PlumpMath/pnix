//! plain Rust의 한계 — 부분평가 결과가 Jones-최적(인터프리터 비만-불변)인지
//! 측정할 표준이 없다.

fn main() {
    // 컴파일러가 인라이닝/상수접기를 하더라도, "인터프리터를 부풀려도 잔여
    // 코드가 한 글자도 안 변한다"는 성질을 검사할 방법이 없다.
    println!("plain Rust: Jones-최적성(인터프리터 비만-불변 residual) 검사가 없다");
}

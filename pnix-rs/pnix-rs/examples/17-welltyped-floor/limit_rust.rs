//! plain Rust의 한계 — rustc의 타입검사 판정을 재검토 가능한 증거로
//! 남기지 않는다.

fn main() {
    // "컴파일이 됐다"는 rustc를 다시 믿는다는 뜻일 뿐, 독립된 신뢰 기반에서
    // 정타입성을 재확인할 방법이 없다.
    println!("plain Rust: 정타입성 판정을 재검토 가능한 증거로 남기지 않는다");
}

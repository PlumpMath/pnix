//! plain Rust의 한계 — 입력 배터리 동등성을 재검토 가능한 내용해시
//! 인증서로 묶는 표준이 없다.

fn main() {
    // #[test]는 개별 assertion 통과/실패만 보고한다. "배터리 전체에 대해
    // 동등하다"는 재검토 가능한 인증서 포맷이 없다.
    println!("plain Rust: 배터리 차분 동등성 인증서(재검토 가능)가 없다");
}

//! plain Rust의 한계 — 코드=데이터가 아니라, S=L 자기해석기가 없다.

fn main() {
    // Rust는 코드가 1급 데이터가 아니다. 프로그램을 값으로 물화(reify)해
    // "자기 언어로 쓴 해석기"로 평가하고 네이티브와 수렴시키는 구조가 없다.
    println!("plain Rust: 코드=데이터(S=L)가 아니라, px 자기해석기 같은 타워를 이룰 수 없다");
}

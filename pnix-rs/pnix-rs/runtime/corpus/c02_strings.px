let name = "pnix"; n = 7; in { hello = "hello, ${name}!"; len = builtins.stringLength name; joined = builtins.concatStringsSep "-" [ "a" "b" "c" name ]; sub = builtins.substring 0 3 "abcdefgh"; interp = "n=${builtins.toString n} sq=${builtins.toString (n * n)}"; }

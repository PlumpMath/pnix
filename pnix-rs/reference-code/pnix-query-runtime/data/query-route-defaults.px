{
  # standalone kernel route defaults owner.
  # query-routes.px의 query-context는 이제 각 route entry에서 명시해야 한다.
  # 이 파일은 legacy Doghouse query-context를 standalone Pnix context로 정규화한다.
  query-context-rewrite-rules = [
    { from = "Doghouse."; to = "Pnix."; }
  ];
}

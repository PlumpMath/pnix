# Foundation example

This example exercises PNIX source through the ClojureScript runtime and then
through the generated CommonJS boundary.

```sh
../../bin/build-cljs
node node.js
node ../../pnix-cljs/dist/pnix-cljs.js program.px
```

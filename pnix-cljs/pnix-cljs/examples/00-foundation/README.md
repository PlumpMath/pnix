# 00 — foundation (cljs seed)

First step of the **core 00–06** path (see [FOUNDATION_PATH.md](../FOUNDATION_PATH.md)).

Exercises PNIX source through the ClojureScript runtime / CommonJS boundary.

```sh
cd pnix-cljs
./bin/build-cljs
node pnix-cljs/examples/00-foundation/node.js
# or:
# node pnix-cljs/dist/pnix-cljs.js pnix-cljs/examples/00-foundation/program.px
```

Catalog index: [../README.md](../README.md). Cross-host balance:
monorepo `examples/EXAMPLES_BALANCE.md`.

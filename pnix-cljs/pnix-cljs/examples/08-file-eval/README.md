# 08 — 파일 평가 (`evalFile`)

## 무엇을

문자열뿐 아니라 **파일 경로**로 `.px` 를 읽는다. Node 라이브러리 export 의
`evalFile` / `evalFileValueJson` 표면.

## 실행

```bash
cd pnix-cljs
./bin/build-cljs
node pnix-cljs/examples/08-file-eval/pnix_cljs_way.js
```

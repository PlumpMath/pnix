# 10 — 값 JSON 투영

## 무엇을

`evalValueJson` / `evalSourceJson` 으로 **관측용** JSON 투영을 본다.
JSON 은 언어 값 자체나 타입 권위가 아니다 (README 제품 문구와 동일).

## 실행

```bash
node pnix-cljs/examples/10-value-json-projection/pnix_cljs_way.js
```

# 04 — JS 에 pnix 임베드 (host-main)

## 무엇을

호스트 프로그램(JS)이 드라이버이고, `.px` 가 게스트 표현이다. 반대 축(pnix-main
REPL)은 모노레포 flake `.#pnix-cljs-pnix` 계열.

## 실행

```bash
node pnix-cljs/examples/04-js-embed-pnix/host_main.js
```

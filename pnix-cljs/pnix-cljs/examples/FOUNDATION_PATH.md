# Foundation path — pnix-cljs

Minimal reading order for day-to-day use (not the full research ladder):

1. **`00-foundation`** — eval a few `.px` forms via the Node module.
2. **`01-pure-eval-boundary`** — why plain `eval` is not a guest boundary.
3. **`02-host-library-import`** — local library export, not npm registry.
4. **`03-outcome-projection`** — structured outcomes.
5. **`04-js-embed-pnix`** — host-main orientation.
6. **`05-experimental-honesty`** — non-claims.
7. **`06-meta-pair-boundary`** — product vs meta half.

Deeper specialization / machine / oracle examples live on **pnix-clj** / **pnix-hy**;
this host adds slices only when the surface is real.

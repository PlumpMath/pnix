# 14 — tryEval

## 무엇을

`builtins.tryEval` 로 throw/실패를 구조화된 `{ success, value }` 로 받는다.
타입 오류 catch 범위는 호스트·Nix 버전마다 다를 수 있음 — seed 수준 스모크.

## 실행

```bash
node pnix-cljs/examples/14-tryEval/pnix_cljs_way.js
```

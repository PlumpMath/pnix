# 06 — meta pair boundary

## 무엇을

모노레포 이중 축:

| half | role |
|------|------|
| **pnix-cljs** (this package) | pnix runtime: parse/eval, Node export |
| **cljs-meta** (`pnix-cljs/cljs-meta/`) | host-language meta / fixed-point mechanism |

예제는 product 쪽만 보여 준다. meta 게이트는 `cljs-meta` README / bin을 본다.

## 관련

- monorepo `README.md` — five hosts as pairs
- `pnix-cljs/cljs-meta/README.md`

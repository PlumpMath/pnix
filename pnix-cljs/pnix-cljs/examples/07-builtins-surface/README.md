# 07 — builtins 표면 (seed)

## 무엇을

게스트 표현에서 `builtins.typeOf`, `attrNames`, `getAttr` 등 **인정된 seed builtins**
를 돌려 본다. clj 전 레인·전체 Nix 패리티를 주장하지 않는다.

## 실행

```bash
cd pnix-cljs
./bin/build-cljs   # dist 필요 시
node pnix-cljs/examples/07-builtins-surface/pnix_cljs_way.js
```

# pnix-clj WIKI (generated — do not edit)

Regenerate with `clojure -M:wiki`; the gate runs `clojure -M:wiki check`
and fails on drift OR a `:landed` roadmap item whose capability is not
actually wired. **Grep this file before implementing anything** — it is
the anti-duplication source of truth.

epic: 🗼 metacircular level — role-translated from pnix-hy onto Clojure(clj-meta)↔pnix

counts: capabilities=46 · roadmap-landed=85 · in-flight=0 · planned=3

## Implemented capabilities (report-artifact registry)

Each is regression-pinned by its `*/report` and runnable via its alias.

| capability | run | summary |
|---|---|---|
| `arith-proof` | `-M:arith-proof` | PROVEN equivalence for the pnix arithmetic fragment (+, -, *, int, var) via |
| `bool-proof` | `-M:bool-proof` | PROVEN equivalence for the pnix boolean fragment (true/false, ! && \|\| ->, |
| `cached-eval` | `-M:cached-eval` | Content-addressed evaluation cache (roadmap M6), role-translated from |
| `cas` | `-M:cas` | §3 — canonical normalization + content-addressed TERM store (BUILD 1st of the |
| `cegis` | `-M:cegis` | Candidate GENERATOR #2 -- CEGIS refinement (counterexample-guided inductive |
| `classfile-receipt` | `-M:classfile-receipt` | JVM class-file artifact receipts -- content hashes of the classes clj-meta emits, so a compiled artifact's identity is verifiable and stable. |
| `clojure-form` | `-M:clojure-form` | Clojure-form fixture lane: pnix sources whose lowered Clojure form is pinned, exercising the pnix->Clojure-form projection. |
| `clojure-projection` | `-M:clojure-projection` | pnix -> Clojure projection report over a fixture corpus (the lowering lane's surface, pinned per case). |
| `coverage` | `-M:coverage` | Dynamic evaluator coverage over the repo-owned source corpus -- which ops, builtins, and branches the fixtures actually exercise. |
| `determinism` | `-M:determinism` | Evaluation determinism: parse/eval each source K times and assert stable AST + result hashes (no hidden nondeterminism). |
| `emit-form-roundtrip` | `-M:emit-form-roundtrip` | Clojure-form emit <-> tools.analyzer roundtrip: a lowered form re-analyzed must round-trip, checking the emitter against the JVM analyzer. |
| `form-analysis` | `-M:form-analysis` | Clojure-on-Clojure AST-pass lane (deep-research F4) — a reusable static |
| `forward-reference` | `-M:forward-reference` | (no docstring) |
| `futamura` | `-M:futamura` | Futamura 2nd & 3rd projections, on top of the 1st (pnix-clj.specialize). |
| `generate` | `-M:generate` | The FIRST candidate GENERATOR for the self-* loop -- observational- |
| `grammar-fuzzer` | `-M:grammar-fuzzer` | Grammar fuzzer: generates positive (must-parse/eval) and negative (must-reject) pnix sources to stress the parser + evaluator beyond the fixed corpus. |
| `live-oracle` | `-M:live-oracle` | Live Nix oracle: differential-tests pnix values against nix-instantiate when it is available, grounding the runtime in real Nix semantics. |
| `machine` | `-M:machine` | M7 — pnix abstract machine, DERIVED from the definitional tree-walk |
| `mirror-chain` | `-M:mirror-chain` | §6.6-6.7 — mirror DRIFT events + repeated-run chain-convergence stability |
| `mirror-error` | `-M:mirror-error` | Error-path mirror corpus: sources that must HOLD/ERROR, checking all lanes agree on the same failure (reason/error), not just on successes. |
| `mirror-pair` | `-M:mirror-pair` | The core 4-lane cross-check corpus: every source must collapse to one value across the direct evaluator, clj-meta bytecode, .px runtime, and pnix mirror. |
| `persist` | `-M:persist` | Durable backing for the evidence-store spine -- content-addressed on-disk |
| `property-fuzzer` | `-M:property-fuzzer` | Property-based differential fuzzer (deep-research F3 extension). Where |
| `purity` | `-M:purity` | §9 — purity / determinism as EVENTS (spine BUILD 5th; see docs/SPINE_ROADMAP.md). |
| `reflect` | `-M:reflect` | §10 + §13.1 — Clojure namespace/Var/metadata reflection SNAPSHOTS + classpath/ |
| `replay` | `-M:replay` | Replay / audit -- independently RE-VERIFY a persisted witness later, in a |
| `rust-batch` | `-M:rust-batch` | Rust-grounded invariance corpus: fixtures ported from the Rust pnix implementation, pinning cross-implementation value agreement. |
| `safe-eval` | `-M:safe-eval` | Resource-limited pure evaluation tier (roadmap M5), role-translated from |
| `search` | `-M:search` | §17 (+ §3c open-term summary) — content-address + event + structural- |
| `self-improve` | `-M:self-improve` | The self-* loop BODY -- evaluate a batch of candidate self-modifications and |
| `self-mod-gate` | `-M:self-mod-gate` | §14.3 — self-modification gate. Operationalizes the pnix-clj constitution's |
| `smoke` | `-M:smoke` | Fast smoke report over the ground-truth oracle cases -- the quickest all-lanes-agree sanity signal. |
| `snapshot` | `-M:snapshot` | §8 — snapshot determinism (spine BUILD 4th; see docs/SPINE_ROADMAP.md). |
| `specialize` | `-M:specialize` | pnix partial evaluator — Futamura stage 1 (roadmap M1, first slice). |
| `stage15-exec` | `-M:stage15-exec` | Stage-15 execution lane: runs the staged plan and records its execution receipt. |
| `stage7-core` | `-M:stage7-core` | Stage-7 core self-* fixtures: the meta-circular self-hosting core sources pinned for regression. |
| `store` | `-M:store` | §5 — append-only EVENT log (spine BUILD 2nd; see docs/SPINE_ROADMAP.md). |
| `strict-audit` | `-M:strict-audit` | Strict-Nix-typing audit (audit-only, never changes results): classifies which sources WOULD fail under strict Nix typing; also the strict-gate over strict-ok sources. |
| `synthesize` | `-M:synthesize` | Reverse projection (roadmap M3): a whitelisted CORE of ordinary Clojure |
| `tower` | `-M:tower` | Meta-circular tower entrypoint (roadmap M2). |
| `translation-validation` | `-M:translation-validation` | Per-compilation translation validation: for each source-form -> clj-meta bytecode lowering, verify semantic preservation for THAT run (Necula/Pnueli style), not whole-compiler correctness. |
| `trust` | `-M:trust` | Trust / provenance receipts: content-addressed artifact trust chain over the compiled + runtime evidence. |
| `value-roundtrip` | `-M:value-roundtrip` | Value roundtrip: a normalized pnix value -> canonical Clojure form -> re-created value, checking meaning-preserving value projection. |
| `weval` | `-M:weval` | F8: weval-shaped IR-level 1st Futamura projection on an interpreter BODY (Fallin, weval, PLDI 2025; Truffle PE, PLDI 2017) -- pc-as-context dispatch elimination, STATIC residuals only, hand-placed boundaries; an architectural proof that clj-meta hosts IR-level PE, NOT a performance program. |
| `witness` | `-M:witness` | §15 — explicit witness schema + admission lattice (spine BUILD 8th, CAPSTONE; |
| `witnessed-run` | `-M:witnessed-run` | Spine INTEGRATION -- one witnessed run that ties the evidence-store spine to |

## Roadmap — landed

| id | capability | title |
|---|---|---|
| `m1-specialize` | `specialize` | M1 · Futamura 1st projection — partial evaluator (specialize → residual pnix → JVM bytecode) |
| `m2-tower` | `tower` | M2 · self-hosting tower — one source, 4 substrates, collapse to one value |
| `m3-synthesize` | `synthesize` | M3 · reverse projection — Clojure form → pnix (bidirectional) |
| `m4-capabilities` | — | M4 · machine-generated capability index + drift gate (docs/CAPABILITIES.md) |
| `m5-safe-eval` | `safe-eval` | M5 · pure resource-bounded sandbox — fuel + static purity + capability gate |
| `m6-cached-eval` | `cached-eval` | M6 · content-addressed evaluation cache (position-stripped AST hash + epoch) |
| `translation-validation` | `translation-validation` | per-compilation translation validation (source-form ↔ bytecode) |
| `emit-form-roundtrip` | `emit-form-roundtrip` | emit ↔ parse form roundtrip (structural equality) |
| `value-roundtrip` | `value-roundtrip` | value roundtrip (meaning preservation across projection) |
| `grammar-fuzzer` | `grammar-fuzzer` | grammar fuzzer (positive/negative source generation) |
| `determinism` | `determinism` | evaluation determinism (repeat-eval hash stability) |
| `coverage` | `coverage` | dynamic evaluator coverage (ops/builtins/branches) |
| `strict-audit` | `strict-audit` | strict-typing audit (would-fail-under-strict-Nix classification) |
| `live-oracle` | `live-oracle` | live oracle (nix-instantiate differential when available) |
| `f1-futamura-2nd` | `futamura` | F1 · Futamura 2nd projection — generating extension (cogen-free, Latifi DLS'19); 3rd stated w/ Glück PEPM'09 Thm 1 |
| `f2-jones-optimality` | `futamura` | F2 · Jones-optimality — MEASURED witness in the futamura report: gen(p) compiled-form-size scales with the program and the size/AST ratio stays bounded (converges) — no interpreter floor. Structural measurement, honestly not a mechanized proof. |
| `f3-property-fuzzer` | `property-fuzzer` | F3 · property-based generative differential fuzzer (test.check) — random pnix exprs, 4-lane collapse property, SHRINKS any divergence to the minimal source. Two properties: cross-lane collapse (typed generator over arith/let/if/lists/builtins/attrsets/bool) AND specializer soundness (partial eval preserves meaning under any static split). Found 2 real bugs: clj-meta nested-let shadowing [fixed], host-parser let/if-as-operator-RHS leniency [filed] |
| `f4-analyzer-pass-lane` | `form-analysis` | F4 · tools.analyzer.jvm AST-pass lane (form-analysis) — Clojure-form host-interop surface classification (pure-core verdict) |
| `arith-proof` | `arith-proof` | PROVEN equivalence (not tested) for the pnix arithmetic fragment via canonical polynomial normalization — upgrades specialize (M1) soundness from generative testing to a proof over ALL variable values; honest :unprovable on non-arithmetic residuals (proven-vs-heuristic boundary) |
| `bool-proof` | `bool-proof` | PROVEN equivalence for the pnix boolean fragment via EXHAUSTIVE truth-table evaluation (complete over the finite domain) — companion to arith-proof; proves De Morgan/absorption/distributivity, refutes non-equivalences; honest :unprovable beyond the var bound |
| `converge-synthesize-form-analysis` | — | integration (converge domains) — synthesize (M3) ⟷ form-analysis (F4) cross-check: a form synthesize projects to pure pnix is host-free per the JVM analyzer, and host-interop is refused by both (soundness invariant, gate-pinned) |
| `f7-cogen-collapse` | `futamura` | F7 LANDED · 3rd Futamura projection BUILT (cogen = compiler GENERATOR, cogen-free curried route like F2): ladder D==P1==P2==P3 gate-pinned; cogen-id constant across programs while per-program extension-ids vary; Gluck PEPM'09 collapse (4th projection yields nothing new) MECHANIZED for the curried construction (honest label: by-construction, not the classical self-application theorem) |
| `gate-report-cache` | `smoke` | C LANDED · report-artifact constructive-trace cache (BSàlC §4.2.3): report-for reuses a rendered report WITHIN one JVM run iff the §8 snapshot id matches (fail-safe to fresh); 4 identical-computation deftests (mirror-pair/clojure-projection/clojure-form/forward-reference) route through the cached dispatch — duplicate corpus renders eliminated (mirror-pair deftest 31-43s → 0.0s on hit). determinism/coverage deftests use DIFFERENT args — honestly NOT shared; drift gates never cached; CLI aliases render fresh (per-JVM cache) |
| `d-probe-stack-safety` | `smoke` | D1 LANDED (oracle-confirmed + fixed): Nix evaluates 100k parens/lists, 10k+ lets; we died <1k (parser SOE mislabeled :unsupported-syntax) / ~9-10k (eval). Fix: core/call-on-deep-stack (2GB dedicated thread, bindings conveyed, re-entrant-guarded; SOE -> STRUCTURED :stack-overflow held like Nix) + parser SOE honest :parse-stack-overflow label + eval-let TAIL-POSITION LOOP (let chains constant-stack, fuel/coverage ticks replayed 1:1). Now: parens/list-100k ok, let-10k ok, semantics preserved (mutual ref/recursion/lazy-unused). Gate pin: deep-recursion-nix-parity |
| `d1b-let-chain-scaling` | `smoke` | D1b LANDED · the superlinear time was PER-AST-NODE whole-source sha256 (O(nodes x \|source\|)): parser now hashes ONCE per parse (*source-hash* bound in parse-source*). Oracle parity restored: let-10k 14s->1.3s, let-50k 306s-held->3.2s-OK (Nix: instant 1). The force-chain-recursion hypothesis was WRONG for let (the tail-loop had fixed the descent; the time was the parse hash) — recorded honestly |
| `d-probe-builtins-strictness` | `smoke` | D2 LANDED (oracle-gated, 64-case matrix, 0 divergent): builtins strictness vs nix-instantiate 2.34.7. FIXED 5 oracle-confirmed divergence classes: (1-3) map/filter/mapAttrs NEVER force their function arg on an EMPTY collection (literal or computed; non-empty/non-list forces fn BEFORE the list type error) — asymmetric vs concatMap/any/all/genList/partition/groupBy/zipAttrsWith which force fn even on empty (parity kept); (4) foldl' initial accumulator is LAZY (op that ignores it never evaluates it) while per-iteration/final results stay strict; (5) sort forces each ELEMENT to WHNF (not deep) and skips the comparator on empty — we were too lazy; 2 mirror-pair corpus fixtures (lazy-sort-*) had pinned the too-lazy behavior as ':oracle-result' WITHOUT oracle confirmation — corrected to nested-poison forms (re-oracle-verified). Mechanism: lazy-builtin-arg? collects those positions unforced; finish-builtin forces exactly where Nix does. builtins.take/drop rows = EXISTENCE divergence (not in real Nix; deliberate lib-under-builtins extension), excluded from the strictness matrix. Gate pin: builtins-strictness-nix-parity. Open: .px/lowering lanes keep their own strictness (corpus stays lane-agreeing; poisoned edges pinned host-lane only) |
| `d-probe-error-taxonomy` | `smoke` | D3 LANDED (oracle-gated, 26-case matrix): catchable-vs-uncatchable vs nix-instantiate 2.34.7 — the tryEval catch-set needed NO change (throw/assert caught incl. nested/if-cond/deepSeq-forced/assert-cond-throw; abort, type errors, 1/0, missing attr (select+getAttr), head-of-empty, elemAt OOB, fromJSON/toString failures, non-function call, missing pattern arg, black-hole self-reference ALL uncatchable, at parity; shallow WHNF + nested tryEval compose). Only 3 divergent rows = the KNOWN R2 lenient defaults (non-bool if/assert condition, '+' string coercion: we evaluate, real Nix type-errors) — NOT a tryEval gap; already audited (Phase A) and held under opt-in strict mode (Phase C) where the matrix reaches 26/26, incl. staying UNCAUGHT through tryEval like real Nix; the default flip stays the R2 Phase D OWNER decision. Gate pin: error-taxonomy-tryeval-nix-parity (42 assertions, both modes) |
| `d-probe-float-parity` | `smoke` | D4 LANDED (oracle-gated, 47-case matrix, 0 divergent): float formatting/semantics vs nix-instantiate 2.34.7. FIXED 2 divergence classes: (1) toString floats = C %.6f over the EXACT binary value (nix-float-str via BigDecimal — Java's own %f pads shortest-repr digits and diverges at 1e30; -0.0 sign-guarded), applied in the evaluator ctx-aware :toString + nix-coerce-to-string AND the lowering lane's coerce-to-string (cross-lane consistent); (2) tokenizer float grammar = Nix's exact regex (([1-9][0-9]*\.[0-9]*)\|(0?\.[0-9]+))([Ee][+-]?[0-9]+)? with the number alternative moved BEFORE punct to reproduce flex maximal munch — .5/1./1.e3/2.5e-2/1.5E+2 now parse; 1e3 and 00.5 stay applications and { }.1 lexes .1 as a float (all held exactly like Nix). Confirmed already-parity: toJSON shortest-round-trip floats, mixed int/float promotion + / semantics, int-float ==, floor/ceil negatives + int result type, fromJSON float typing, float div-by-zero held. concatStringsSep non-string coercion leniency = the KNOWN R2-audited boundary (strict mode holds :string-list-builtin-non-string-element), Phase D owner-gated. Gate pin: float-parity-nix-parity |
| `d5-corpus-oracle-sweep` | `smoke` | D5 LANDED (oracle-gated sweep): ALL 232 corpus :oracle-result pins re-verified against nix-instantiate 2.34.7 (mirror-pair 199 + ground-truth 20 + literals 7 + stage7-core 5 + import-module 1, import fixtures materialized to a temp dir and evaluated with real CWD import). Final: 183 match / 0 PIN-WRONG / 39 not-in-nix (deliberate lib-under-builtins extensions) / 6 ctx-relaxation (documented: appendContext keys not validated as store paths) / 4 path-frontier (documented: paths evaluate to literal text, no CWD resolution). The sweep CAUGHT one real divergence: Nix list elements are expr_select ONLY — every operator (binary, unary !/-, and ?) is a syntax error in element position; our parser parsed full expressions ([ 1 + 2 ] => [3], oracle: syntax error). FIXED: parse-list now parses elements at the postfix level with *select-only* (parse-expr resets it at every nested full-expression boundary); 4 [2 / 0] zipListsWith fixtures parenthesized (pins unchanged, corpus re-verified) AND the repo's OWN .px runtime sources contained 3 real-Nix-invalid list literals ([ acc.cur + c ] in pnix-lisp.px, [ sig // {...} ] + [ n // {...} ] in pnixc-pnix runtime.px) that had been leaning on the lenient parser — parenthesized in the runtime resources AND the import-source checkouts (re-vendor safe). parametric-mirror-plate.px unterminated-${..."..."}-interpolation held is PRE-EXISTING (HEAD parser identical), a separate tokenizer frontier. Motivation: 3 non-oracle-verified pins had already been caught ad hoc (D2 lazy-sort x2, D4 toString "3.14") — the corpus pin set is now 100% oracle-accounted. Gate pin: parser-list-elements-select-only-nix-parity |
| `d6-probe-operator-precedence` | `smoke` | D6 LANDED (oracle-gated, 30-case distinguisher matrix, 30/30): expr_op precedence ladder vs nix-instantiate 2.34.7. FIXED 3 oracle-confirmed divergence classes: (1) == != and < > <= >= are TWO separate NONASSOC levels (1 == 1 == true / 1 < 2 < 3 are Nix SYNTAX ERRORS — we returned true; == binds looser than < so true == 1 < 2 is true — we held); (2) `?` moved OUT of postfix to its Nix operator level (looser than application/select/unary-minus): f { } ? a is (f { }) ? a (we mis-grouped), RHS is an ATTRPATH ({ a.b = 1; } ? a.b — desugared to existing if/has-attr/select ops so no new evaluator op), chains ({} ? a ? b) are LEGAL in 2.34 (NOT nonassoc-enforced — oracle surprise), and `?` on a non-attrset is FALSE not an error (1 ? a; builtins.hasAttr stays strict, errors like Nix) — evaluator eval-has-attr + lowering lane guarded alike; (3) `!` operand parses at the add level (Nix ! is precedence 8, LOOSER than + - * / ++ ?: ! { } ? a = !({ } ? a), ! a + b = !(a + b)). NOT changed (honest): // and ++ keep the shared level — their Nix placement (++ at 5, // at 9, both right-assoc) is OBSERVATIONALLY EQUIVALENT here: update/concat are associative and every precedence-distinguishing tree has both sides erroring (type-incompatible operands); D5's *select-only* flag became structural (`?` no longer lives in postfix) and was removed. Gate pin: operator-precedence-nix-parity. Open (px lane): .px-side `?`-on-non-set / attrpath parity unprobed — corpus keeps lane-agreeing shapes only |
| `d7-string-nesting-tokenizer` | `smoke` | D7 LANDED (oracle-gated, 21-case matrix 21/21): string lexing rebuilt as a BALANCED SCANNER shared by the tokenizer and the template splitters — a regex cannot lex Nix strings because a ${...} splice is a full expression that may contain strings of BOTH kinds recursively ("a${"n${"e"}s"}t"), comments with braces inside ("a${ /* } */ "b" }c" and #-comments), brace pairs, and identifier-tail apostrophes (foo' consumed wholesale so '' openers are never mistaken). Real-Nix /* */ BLOCK COMMENTS also landed (missing entirely pre-D7; # only). This closes the D5-diagnosed frontier: parametric-mirror-plate.px (665KB, held since before this session on 'unterminated string interpolation') now parses — the ENTIRE .px runtime corpus tokenizes, gate-pinned on a big-stack thread. Documented leniency KEPT: \"-delimited strings inside splices (real Nix rejects; existing dialect sources rely on it — template-expr-source unescape preserved). Gate pin: string-nesting-tokenizer-nix-parity (50 assertions) |
| `d8-px-lane-hasattr-parity` | `smoke` | D8 LANDED (cross-lane probe of the D2-D7 edges through run-source): ONE active value mismatch found and fixed — the .px runtime parser still had `?` at the postfix level (the exact pre-D6 host bug), so (x: { a = 1; }) { } ? a gave a :rejected :px-runtime-value-mismatch receipt (host true, px {a=1}). FIX mirrors D6 into the px parser (runtime resources AND the pnixc-pnix import-source checkout, adapted to its older seg surface): `?` moved out of parse_postfix to a parse_has_attr level between parse_mul and parse_apply, ATTRPATH RHS desugared identically (e ? a.b == if e ? a then e.a ? b else false, ident/string/interp/dynamic segs), chains legal. All probed `?` shapes now :accepted :all-lanes-agree incl. attrpath rows that used to hold. Remaining px/lowering-lane gaps from the probe are GRACEFUL HOLDS (never value mismatches), filed honestly: px float grammar (.5/1./exponents) + nested-string tokenizing + cmp nonassoc + D2 builtin strictness edges; lowering lane lazy-fn strictness (clj-meta-eval-failed on D2 empty-collection edges) + dotted attrset-binding keys (:unsupported-lowering-attr-key — pre-existing, NOT the D6 desugar which lowers fine). Gate pin: px-lane-hasattr-precedence-parity |
| `d9-lowering-lane-d2-strictness` | `smoke` | D9 LANDED (lane-parity, continues D8's filed backlog): the D2 strictness semantics now hold in the LOWERING (clj-meta) lane too — previously the lowered forms evaluated the function/init form EAGERLY so the D2 empty-collection edges held with :clj-meta-eval-failed. Fixes (4 sites in lowering.clj, mirroring D2): map/filter/mapAttrs defer the fn form into the non-empty arm (empty list/attrset returns []/{} without ever evaluating it); foldl' passes the initial accumulator as a LAZY SLOT under the lane's existing slot convention (the operator body forces on USE exactly like list-element slots; the final result stays forced, so foldl' op (1/0) [] holds identically on both lanes — the strict fold). Result: all D2 edges agree host==clj-meta (map/filter/mapAttrs empty + foldl' ignored-acc now :all-lanes-agree where px supports the source); px lane remains graceful-held on throw-form sources (its filed backlog). Gate pin: lowering-lane-d2-strictness-parity |
| `d10-attrset-binding-merge` | `smoke` | D10 LANDED (oracle-gated, 16-case matrix 16/16 + 3 edge probes): attrset binding MERGE parity — real Nix merges bindings AT PARSE TIME (addAttr): static dotted paths nest, and two bindings of one key merge iff BOTH values are attrset LITERALS (rec or not — rec-literal merge oracle-confirmed, merged set keeps rec if either side had it); an expression/variable/inherit collision is 'attribute already defined', a PARSE error. We had a WRONG-VALUE class bug ({ a.b = 1; a = { c = 2; }; } silently LOST b) and held too strictly on the reverse order. FIX: parser-level merge-attr-bindings (static-path->nested + literal-merge with recursion into children), exactly Nix's parse-time semantics; dynamic keys and dynamic-segment paths pass through to the evaluator's existing eval-time machinery unchanged; rec scoping survives (rec { a.b = x; x = 5; } and post-merge sibling refs oracle-confirmed). BONUS: the lowering lane's dotted-binding frontier (:unsupported-lowering-attr-key, filed in D8) closed for FREE — the parser now hands every lane canonical nested literals (clj-meta lane ok; px lane parses dotted bindings itself → its filed backlog). Gate pin: attrset-binding-merge-nix-parity |
| `d11-px-lane-eq-rel-nonassoc` | `smoke` | D11 LANDED (px backlog, mirrors D6): the px runtime parser's single left-assoc comparison level split into TWO NONASSOC levels (== != looser than < > <= >=) via a new parse_bin_nonassoc, in the runtime resources AND the pnixc-pnix import-source checkout. `true == 1 < 2` upgraded from px-held to :all-lanes-agree; comparison chains stay held at parse exactly like the host (self-corpus scanned: no comparison chains). Remaining px backlog: float grammar, nested-string tokenizing, D2 builtin strictness edges, dotted attrset bindings. Gate pin: px-lane-eq-rel-nonassoc-parity |
| `d12-px-lane-float-grammar` | `smoke` | D12 LANDED (px backlog, mirrors D4): the px lexer only knew digit.digit floats — it now carries the Nix float grammar (dot-leading .5 with path/... branches keeping priority, trailing-dot 1., exponents only after a dot, sign only immediately after e/E) and parse_number NORMALIZES for fromJSON (.5 -> 0.5, 1. -> 1.0, 1.e3 -> 1.0e3), in the runtime resources AND the pnixc-pnix import-source checkout. All float literal forms now :all-lanes-agree (incl. inside lists and mixed arith). SIDE-DISCOVERY filed as D13: BARE default-scope builtins (toString/map/isNull/...) do not lower — builtins.toString 1.5 agrees at "1.500000" but bare toString 1.5 is :clj-meta-eval-failed and bare map leaks clojure.core/map (caught by the receipt value-mismatch guard, held not wrong). Gate pin: px-lane-float-grammar-parity |
| `d13-lowering-bare-builtins` | `smoke` | D13 LANDED (filed during D12): the lowering lane left BARE default-scope builtin names as unresolved vars — bare toString failed to eval and bare map LEAKED clojure.core/map (the receipt value-mismatch guard held it, no false accept). FIX: builtin-select-name treats an UNSHADOWED bare builtin var (map/toString/isNull/baseNameOf/dirOf/removeAttrs; throw/abort already dedicated; import/scopedImport stay held) exactly like its builtins.X select — every call special-case benefits automatically — and the :var VALUE position resolves through builtins-attrset, placed BEFORE the with-scope branch because the static base scope beats `with` (oracle + host agree: with { map = 1; }; map (x: x) [ 2 ] is [2]) while `let` shadows via *lexical-vars*. All bare forms now agree host==clj-meta incl. toString floats at %.6f; px lane holds gracefully (it does not bind unprefixed builtins — px backlog). Gate pin: lowering-bare-builtins-parity |
| `d14-px-lane-bare-builtins` | `smoke` | D14 LANDED (px backlog, completes D13 across lanes): the px evaluator's initialEnv now seeds the bare default-scope builtin set (map/toString/isNull/baseNameOf/dirOf/removeAttrs — throw/abort/import/scopedImport were already bound) from its nativeBuiltins, in the runtime resources AND the pnixc-pnix import-source checkout (adapted: the older checkout lacks dirOf/removeAttrs natives, so those pass through to host builtins directly). PRECEDENCE FOR FREE: the px Var branch checks env BEFORE with-scopes, so `let` shadows and `with` does not — exactly the D13 oracle order. Since the px lane executes ON the host, bare toString floats inherit D4's %.6f. All bare-builtin shapes now :all-lanes-agree incl. with/let shadowing rows. Remaining px backlog: nested strings, D2 strictness edges, dotted attrset bindings. Gate pin: extended lowering-bare-builtins-parity |
| `d15-px-lane-d2-strictness` | `smoke` | D15 LANDED (px backlog — D2 now agrees across ALL THREE executing lanes): the px evaluator's Apply tail forced EVERY native argument ('Native builtins are strict, so the thunk is forced HERE'), making the D2-lazy positions over-strict on that lane — the wrapper-level fix alone was UNREACHABLE (diagnosed by bisecting: guest lambdas/let were lazy, native application was the forcer). FIX: mk_lazy_native/is_lazy_native markers in the px evaluator — a lazy-native receives its next argument UNFORCED (both the evalApply tail and applyValue honor it) and forces/marshals internally in the Nix order: map/filter/mapAttrs/sort skip the fn on an empty collection (ensureFunction moved inside the non-empty arm, marshal_to_host at the boundary), and foldl'/foldl take op strictly (Nix forces it even on empty) but the INITIAL ACCUMULATOR lazily via a native loop with per-iteration + final forcing (the strict fold). 12/12 receipt matrix :all-lanes-agree incl. regressions. HONEST BOUNDARY: landed in the runtime RESOURCES only — the pnixc-pnix import-source checkout's evaluator predates the call-by-need generation (its Apply evaluates every argument eagerly), so adapting D15 there means porting the modern evaluator wholesale; a re-vendor must take the resources copy. Gate pin: px-lane-d2-strictness-parity |
| `d16-px-lane-dotted-bindings` | `smoke` | D16 LANDED (px backlog, mirrors D10): the px parser only knew single-key attrset bindings — a static dotted path (ident/plain-string segments) now NESTS and MERGES with sibling attrset literals exactly like Nix's parse-time addAttr (attr_path_nest + push_assign/merge_attr_values in the px parser; rec flag ORs across merged literals; non-literal collisions are 'attribute already defined' parse errors; dynamic/interp keys keep single-key behavior). The px EVALUATOR is untouched — only canonical single-key entries reach it, the same architecture as the host D10 fix. 8/8 receipt matrix :all-lanes-agree incl. cross-order merges, deep paths, rec scoping through the merge, and dotted STRING keys staying unsplit. Runtime resources only (the checkout parser predates this shape — re-vendor takes the resources copy, as with D15). px backlog remaining: nested-string tokenizing (the last item). Gate pin: px-lane-dotted-binding-parity |
| `d17-px-lane-string-nesting` | `smoke` | D17 LANDED (the LAST px-backlog item, mirrors D7): the px string lexer ended the token at the first unescaped quote, so any string inside a ${...} splice broke it. FIX (runtime resources): the lexer copies a splice VERBATIM as ONE substring element (UTF-8 safe — char-wise copy would shred multibyte; escapes left raw for split_interp) using find_matching_brace as the SHARED balanced scanner, which also learned #/'/* */'-comment skipping (braces in comment bodies no longer close splices — same fix serves the lexer AND split_interp, the D7 shared-scanner principle); `\$` is preserved through lexing so split_interp distinguishes an escaped dollar from a real splice (pre-existing loss D17 exposed); and the px expr lexer gained real-Nix /* */ block comments (bcomment mode — the host got these in D7). 9/9 receipt matrix :all-lanes-agree incl. nested templates, comment-in-splice, escaped-$, UTF-8 splices, and regressions. NOTE: px has no ''-indented strings at all — that is an absent FEATURE (graceful hold), not a nesting bug; filed as future px surface work. With D17 the px lane-parity backlog is EXHAUSTED. Gate pin: px-lane-string-nesting-parity |
| `m7-abstract-machine` | `machine` | M7 LANDED · pnix ABSTRACT MACHINE derived from the definitional evaluator by the functional correspondence (closure conversion + CPS + defunctionalization of continuations — Ager/Biernacki/Danvy/Midtgaard PPDP'03; the technique the 07-08 deep-research verdict named, with trampoline/store-allocated-continuation shortcuts REFUTED 0-3): pnix-clj.machine is a CEK-shaped loop whose control lives in a heap frame stack, call-by-need via the Krivine+memoizing-store refinement (forcing pushes an :update frame IN-LOOP; machine thunks ARE evaluator thunks — same shape+state protocol — so boundary helpers force them transparently). The correspondence transforms CONTROL ONLY: value semantics are the evaluator's own public value algebra (binary-value-result / logical-operand-result / not-value-result / neg-value-result / if-condition-violation — extracted as ONE shared definition, used by BOTH lanes, so they cannot drift). Fragment (v1, honest): the CLOSED lexical core — literals/var/list/let(recursive+lazy+inherit)/if/!/unary-minus/all binary ops/lambda(simple)/apply; everything else (free vars incl. builtins, attrsets, with, templates, patterns) refuses STATICALLY via an iterative free-var walk. Differential gate: 35-source corpus machine==evaluator EXACTLY (ok and held alike, incl. blackhole :infinite-recursion, D18 helds, laziness rows). DEPTH (the D1c shapes): plus-chain-100k machine ok 100001 (~1.2s) where the tree-walk lane HOLDS; nested-list-100k ok, realize ITERATIVE (explicit stack — force-deep recursion is itself a D1c site), depth verified 100000 iteratively; 256KB-thread witness: same AST, machine ok, tree-walk StackOverflowError — constant JVM stack BY CONSTRUCTION, demonstrated. Tree-walk keeps its graceful bound (per the verdict, that IS Nix parity). Follow-ups filed: fragment growth (attrset/select/with/templates/builtins env), a :machine report capability, fuel/coverage hooks. Gate pins: machine-derivation-agrees-with-evaluator, machine-constant-stack-beyond-treewalk |
| `m7b-machine-fragment-growth` | `smoke` | M7b LANDED · machine fragment growth (attrset/select/has-attr/assert/with/string-template): the loop gained a fourth mode — :eval/:value/:force/:unwind — where :force is the memoizing-store thunk entry (var hits, with-scope hits, select slots all route through it) and :unwind propagates a held outward, memoizing pending :update frames AND letting a [:select-attr] frame with an `or` default CATCH a missing-attr-class held exactly like eval-select (a delimited-catch defunctionalized — the shape tryEval will need later). New value-algebra seams shared with the evaluator (still control-only transformation): assert-condition-violation, interpolation-value-result (the `${...}` coercion incl. __toString/outPath — machine closures ARE evaluator closures so apply-callable works through the seam), attrset-value? promoted, default-env-names. Fragment rule refined: a free var refuses statically IFF it names a default-env binding (builtins/bare scope/__curPos — the machine deliberately has no builtins env); other free vars run and hold :unbound-var exactly like the evaluator. attrsets: static keys only (D10-canonical dotted paths nest at parse; surviving :path bindings and dynamic keys refuse), rec via the same env-ref knot as let, plain inherit from the enclosing env. realize-deep gained iterative MAP realization (sorted keys, RealizeMapExit). Differential corpus grew 35→78 rows, ZERO divergence first run (incl. rec knots, or-default catch vs non-miss helds, lexical-beats-with, closure-captures-scope, __toString interpolation, template coercion helds). 256KB witness extended: nested-attrs-3k machine-ok vs tree-walk-SOE (realize side). ★FILED while probing: the PARSER is measurably superlinear on nested attrset literals (20k ≈ 52s parse; machine run+realize on the parsed AST 0.2s) — a parser observation, oracle-probe before working it. Still refused: builtins env, param patterns, dynamic keys, import, paths — next growth. Gate pins: machine-derivation-agrees-with-evaluator (v2 corpus), machine-constant-stack-beyond-treewalk (attrs row) |
| `m7c-machine-builtins-env` | `smoke` | M7c LANDED · machine runs under the DEFAULT ENV — builtins resolve as vars and via builtins.X; application of anything that is not a simple closure (builtins w/ partial application + the D2 lazy-argument positions + finish, pattern closures arriving as values, host fns, and the :call-target-not-callable error) delegates WHOLE to the evaluator's public apply-callable — one definition, zero drift; the builtin's INTERNALS run on the evaluator's own recursion, a HAND-PLACED value-algebra boundary (weval discipline), not machine control. tryEval = a [:try-eval] frame: the :unwind mode catches exactly the D3 catchable reasons (:throw-builtin-called/:assertion-failed), everything else stays uncatchable — the second delimited catch the M7b :unwind restructure predicted. Select gained the [:select-finish] frame sharing the new nullary-builtin-result seam (eval-select rewired to the same definition); __curPos mirrors the evaluator's :var special (wins even over a lexical shadow, bug-for-bug) via the promoted source-position. The static walk SIMPLIFIED: no free-var/bound-set analysis at all — an unbound name holds :unbound-var at run time exactly like the evaluator; remaining static refusals = param patterns, dynamic attr keys/segments, dynamic path bindings, :path, :import, non-simple let bindings. Differential +44 rows, ZERO divergence first run (D2 strictness edges THROUGH the delegation boundary, tryEval matrix incl. uncatchables, bare-name/with/let scope priority (D13 oracle), toString %.6f, purity-gated readFile, nullary nixVersion, 1 2 not-callable). Next growth: param patterns (native frames, NOT delegation — the body must stay machine control), dynamic keys, paths/import; then instrumentation parity + a :machine report capability. Gate pin: machine-derivation-agrees-with-evaluator (v3 corpus) |
| `m7d-machine-pattern-lambdas` | `smoke` | M7d LANDED · pattern lambdas NATIVE in the machine (not delegated — the D19 verdict made delegation wrong to want: defaults and body must stay MACHINE control): a pattern closure's application evaluates the argument to WHNF under a [:pattern-bind] frame, then the binding itself is value-level — the D19 guards verbatim (attrset / required-in-pattern-order / extra-keys-unless-ellipsis, same held reasons) and the KNOT-TIED env where each missing formal binds a lazy MACHINE thunk (::mast=default ::menv=env-ref) forced in-loop like every machine thunk, so a default cycle hits the shared blackhole (:infinite-recursion) and an unused throw-default never runs; @as binds the actual argument only. Simple-closure application gained the apply-callable base-env parity (env-ref deref — evaluator rec-attrset closures arriving as values). Static walk: param-pattern refusal REMOVED; lambda children include pattern defaults. Differential +22 rows ZERO divergence first run (D19 matrix through the machine + pattern closures via builtins.map / functionArgs metadata / rec / tryEval uncatchability). Remaining refusals: dynamic attr keys/segments, :path, :import, non-simple let bindings — the fragment now covers ~the whole static-key language. Next: dynamic keys, paths/import, instrumentation parity + :machine report capability. Gate pin: machine-derivation-agrees-with-evaluator (v4 corpus) |
| `m7e-machine-dynamic-keys` | `smoke` | M7e LANDED · dynamic attr keys native in the machine (D20 semantics): the all-static attrset keeps its one-step build (no duplicate is possible — parse-checked); a dynamic key switches to the FRAME-DRIVEN path — attrs-transition consumes the static prefix inline (with the D20 duplicate check) and each dynamic key expression evaluates under an [:attrs-key] frame (shared attr-key-value-result string check + duplicate check + rec env'/env-ref threading exactly like eval-attrs' loop). Select and has-attr route dynamic segments through [:select-key]/[:hasattr-key] frames; the static and dynamic select arms share ONE select-transition helper so they cannot drift, and the D20 key-check held is NOT caught by an `or` default (unwind passes [:select-key] through — only [:select-attr]+default catches, and only miss-reasons). Differential +20 rows ZERO divergence first run (dup orders, non-string keys, construction-laziness, rec-sibling-visible dynamics, lazy dynamic values, has-attr key checks). Remaining refusals: dynamic-segment PATH bindings ({ a.${k} = v; } — host threads eval-path-keys+merge-attr-path; future growth), :path, :import, non-simple let. Gate pin: machine-derivation-agrees-with-evaluator (v5 corpus) |
| `d22-dotted-let-bindings` | `smoke` | D22 LANDED (oracle-gated): let bindings are the SAME binds production as attrsets in real Nix. Two parser moves: (1) bare `${"literal"}` attrpath segments FOLD to STATIC keys at parse — oracle: both orders of a folded-key collision are PARSE-time 'attribute already defined' (even unforced inside a let: the old D20 receipt row `let s = { a = 1; ${"a"} = 2; }; in 1` had pinned ok/1 WITHOUT oracle confirmation — the D5/D19 unoracled-pin class, caught and corrected; construction-laziness is now pinned with a genuinely dynamic key, which real Nix leaves unevaluated), `let ${"a"} = 1; in a` is 1, quoted template keys stay dynamic/eval-checked ('dynamic attribute' message class = D20 unchanged); (2) parse-let runs the SAME parse-time addAttr merge as attrsets (path->nested + merge-attr-bindings, entries mapped back to :name bindings) — dotted paths nest and MERGE (let a.b = 1; a.c = 2; literal↔path both orders; a.b.c deep; inherit collisions checked), a DUPLICATE let name is now a parse error (pre-D22 the parser silently let the later binding shadow — oracle-wrong), dynamic SUB-segments keep D21's nested-lazy semantics (let a.${dyn} = 1; in a.b is 1), and a TOP key still dynamic after the fold is real Nix's 'dynamic attributes not allowed in let'. 26-row oracle matrix green; machine follows with ZERO changes (shared parser+AST); lowering/px receipts agree on well-typed rows (px keeps its own-parser backlog for path shapes). Reason moves in existing pins updated honestly (bare-fold collisions: :duplicate-attr → :unsupported-syntax, matching Nix's parse-vs-eval split). Gate pin: dotted-let-nix-parity |
| `d21-dynamic-path-nested-lazy` | `smoke` | D21 LANDED (oracle-gated; filed during M7i, landed same day): dynamic-segment dotted paths desugar at PARSE to nested lazy literals — a.${k} = v IS a = { ${k} = v; }, exactly Nix's addAttr. The fix GENERALIZED D10's static-path->nested (renamed path->nested; the every?-string? caller guard dropped): each dynamic segment becomes the dynamic key of its own nested literal, so (1) a dynamic sub-key evaluates only when its PARENT set forces — { a.${1} = 1; } passes unforced, builtins.attrNames sees ['a'] (the pre-D21 evaluator path branch evaluated ALL segments at construction, too eager); (2) literal↔path siblings merge in BOTH orders ({ a = { c = 1; }; a.${"b"} = 2; }.a.b is 2 — pre-D21 :duplicate-attr, too strict); (3) static prefixes merge like any literal; (4) duplicate dynamic LEAVES still collide at eval via D20. 13-row oracle matrix green. CASCADE: no :path binding survives parse from an attrset, so eval-attrs' path branch and the machine's M7i [:attrs-path] frames become DEFENSIVE dead code (kept), the machine follows with ZERO changes (shared AST), and the LOWERING frontier for these shapes CLOSED for free (:unsupported-lowering-attr-key gone — the D20 dynamic-key lowering machinery takes over; clj-meta lane agrees at 3). The px lane is now the honest blocker (:px-runtime-run-error) = filed px backlog (D8–D17 catch-up pattern); rec DIRECT dynamic keys keep their separate lowering frontier. D22 (dotted let) unchanged — parser let bindings are a different production. Gate pin: dynamic-attr-key-nix-parity (D21 block) |
| `m7i-machine-dynamic-path-bindings` | `machine` | M7i LANDED · dynamic-segment PATH bindings ({ a.${k} = v; }) native in the machine — the LAST fragment refusal with runtime semantics: attrs-path-transition consumes static segments inline, each dynamic segment evaluates under an [:attrs-path] frame (D20 string check), and the resolved path merges via the PROMOTED merge-attr-path (one definition with eval-attrs; [:err] = the same :duplicate-attr held), rec threading the TOP key into env'/env-ref exactly like the host. Differential +11 rows zero divergence. The machine now refuses NOTHING the parser can produce except non-simple let bindings (defensive — the parser cannot produce them today, see D22). ★FILED while probing (oracle-confirmed, evaluator follow-ups, NOT machine work): D21 — real Nix treats a.${k}=v as the nested-lazy desugar a = { ${k} = v; } (dynamic sub-keys evaluate only when the PARENT forces: { a.${1} = 1; } passes unforced; attrNames sees ['a']) and MERGES literal↔path both orders ({ a.${"b"} = 1; a.c = 2; }.a.c is 2) — our three lanes are construction-eager and collision-strict (too-strict helds, not wrong values); D22 — dotted LET bindings (let a.b = 1; in a.b is 1 in Nix) are a PARSER frontier (:unsupported-syntax; both machine and evaluator share the parser so the differential already agrees). Gate pin: machine-derivation-agrees-with-evaluator (M7i block) |
| `gate-report-batch` | — | GATE-SPEED LANDED · report batch mode: all 13 gate report kinds render in ONE JVM (report-artifact -main 'batch' + the :report-batch alias + the gate script collapse) — 12 fewer JVM cold starts per gate and the later kinds render on a WARM JIT. Honest accounting: the constructive-trace report cache stays PER-KIND (no cross-kind sharing — the earlier comment draft overclaimed and was corrected); the measured batch run is ~6:00 for all 13 kinds where the separate steps paid ~10s cold start each plus cold-JIT renders. Per-kind failure semantics preserved: every summary prints, any failing kind exits 1 at the end. Single-kind aliases unchanged (CLI compat kept) |
| `m7h-machine-fuzzer-lane` | `property-fuzzer` | M7h LANDED · ASSEMBLY: the machine joins the N-version harness — the property fuzzer's FIFTH generative property (machine-property): random typed pnix expressions must agree machine⇄evaluator EXACTLY (ok and held alike — STRONGER than the cross-lane value-collapse check), with shrinking to a minimal source on divergence, deterministic seed. The fixed 155-row corpus (machine/differential-corpus) pins known shapes; this sweeps the rest of the space randomly every gate — the same harness whose cross-lane property found two real bugs (F3). First sweep: 120 random sources, zero divergence. Report schema v3→v4 (:machine-pass?, :machine-divergence-found). The M-epic assembly shape: derive (M7a) → grow to the language (M7b–f) → promote to a capability (M7g) → wire into the standing verification fleet (M7h) |
| `m7g-machine-report-capability` | `machine` | M7g LANDED · the machine becomes a FIRST-CLASS capability: (1) :machine report-artifact kind — the SHARED differential corpus (machine/differential-corpus, 155 rows, the same list the bootstrap pin runs — one regression surface, no duplicate) plus the 256KB constant-stack witness (machine ok=30001 / tree-walk SOE on the same AST), :ok only when every row agrees and the witness holds, honest labels carried in the artifact (:differential-not-proof, :builtin-internals-on-evaluator-recursion, :fuel-ticks-approximate); deps aliases :machine/:report-machine + a gate step. (2) FUEL parity: the machine loop ticks the SAME *fuel* volatile with the same tagged throw at every :eval entry, so safe-eval-style budgets bound both lanes identically (approximate step counts, honestly not byte-identical). Coverage instrumentation deliberately NOT mirrored (it is the evaluator lane's instrument; the machine is proof-only). Gate pins: machine-report-capability + the corpus-backed machine-derivation-agrees-with-evaluator |
| `m7f-machine-paths-import` | `smoke` | M7f LANDED · :path literals + :import in the machine: path-value is the shared constructor (paths stay literal text — the D5-documented path frontier is a conformance boundary, not machine work); :import mirrors the evaluator's arm verbatim through the SHARED dynamic seam (*import-resolver*/*import-context* — core's in-memory module map binds it for both lanes; resolving is a hand-placed boundary like builtin internals; unwired, both lanes hold :import-evaluation-not-wired identically). Differential +11 rows zero divergence + a shared-resolver-seam pin (machine and evaluator resolve the same 42 under one binding). The fragment now covers the WHOLE language surface except dynamic-segment PATH bindings ({ a.${k} = v; }) and non-simple let bindings. Next: M7g instrumentation parity (fuel/coverage) + a :machine report capability; then the assembly slice (machine as an extra differential lane in the property fuzzer / tower). Gate pin: machine-derivation-agrees-with-evaluator (v6 corpus) |
| `d20-dynamic-attr-key-semantics` | `smoke` | D20 LANDED (oracle-gated, found while preparing M7e): dynamic attr keys — pre-D20 an eval-time key collision SILENTLY OVERWROTE ({ a = 1; "${"a"}" = 2; }.a was 2 — WRONG VALUE; real Nix: 'dynamic attribute already defined', both orders and dyn-dyn) and a non-string dynamic key was COERCED via str ({ }.${1} or "d" was "d"; real Nix type-errors and `or` does NOT catch it). Laziness preserved: keys evaluate at CONSTRUCTION only (an unforced attrset never raises). FIX 3-lane: host = shared attr-key-value-result seam (string ok / ctx-string stays behind the deny-by-default context frontier / else :dynamic-attr-key-not-string) + contains?-check in eval-attrs single-key assoc (:duplicate-attr — only dynamic keys can collide, static-static is a parse error since D10); lowering = attr-key-string runtime helper (plain ex-info, uncatchable) + attrset-pairs dup-checking builder (bare hash-map would even throw its own wrong-shaped duplicate error); px = evalAttrKey isString check + hasAttr collision check in the evalAttrSet fold. Pre-existing lowering frontiers stay honest holds (dynamic PATH bindings :unsupported-lowering-attr-key, rec-dynamic :recursive-dynamic-attr-key-lowering-not-wired) — host keeps the right values, receipts hold gracefully. Gate pin: dynamic-attr-key-nix-parity |
| `d19-pattern-lambda-semantics` | `smoke` | D19 LANDED (oracle-gated, found while deriving M7d): real Nix pattern-lambda semantics — pre-D19 ALL THREE lanes bound defaults EAGERLY in a SEQUENTIAL scope and accepted extra keys. Oracle (nix-instantiate 2.34.7): (1) defaults are LAZY thunks in a KNOT-TIED recursive scope — ({ a ? throw "x" }: 1) { } is 1, ({ a ? b, b ? 2 }: a) { } is 2 (a default sees LATER formals), a default cycle is infinite recursion; (2) REQUIRED formals are checked at application time in PATTERN ORDER before the extra-key check (({ a, b }: a) { a=1; c=1; } reports b, not c); (3) without `...` an extra argument key ERRORS — the parser had always captured :ellipsis?, every evaluator ignored it; (4) the argument must be an attrset; (5) @as binds the ACTUAL argument only. All uncatchable through tryEval. FIX per lane in its own idiom: host = env-ref knot + make-thunk defaults (reasons :lambda-pattern-arg-not-attrset / :missing-lambda-pattern-arg / :unexpected-lambda-pattern-arg, guard order oracle-matched); lowering = pattern-guard runtime helper (plain ex-info, NOT :pnix/catchable → lowered tryEval passes it through like real Nix) + a PROMISE-KNOT emission (each missing formal binds a delay that re-reads every formal slot from a delivered promise — recursive scope in a language whose let is sequential); px = the pnix recursive let knots envF/formalsMap for FREE + mk_thunk defaults + isAttrs/missing/extra guards (resources only — the pnixc-pnix checkout predates pattern closures). 12-row receipt matrix :all-lanes-agree / oracle-reason holds. Gate pin: pattern-lambda-nix-parity |
| `d18-logical-operand-bool` | `smoke` | D18 LANDED (oracle-gated, found while deriving the abstract machine from eval-binary): && \|\| -> require BOOLEAN operands in real Nix ('value is an integer while a Boolean was expected', uncatchable) — pre-D18 ALL THREE lanes leaked truthiness on exactly this class (host `(if (:value left) ...)`, lowering bare Clojure and/or/if, px `l == false then .. else rhs` returning the rhs unchecked): 7/8 probe shapes accepted where Nix errors. The R2 Phase D audit had covered if/assert/!/+ but never the logical operators. FIX mirrors each lane's own Phase D pattern: host strict-violation :non-bool-{and,or,implies}-operand (checked when and only when the operand is evaluated — left BEFORE the right exists, right only past the short-circuit, so false && throw stays false and 1 && throw is the TYPE error), lowering require-bool on both operand forms (and/or keep short-circuiting through the checked bool), px evalIf's `== true / == false / else mk_error` pattern in evalBinary (resources AND the pnixc-pnix checkout's && \|\|; the checkout has no -> arm — older surface, re-vendor takes resources). tryEval boundary matches D3: the type error is uncatchable, a right-side throw stays catchable. Flip measurement: strict gate 278 classified / 269 ok / 0 failed — the px runtime bootstrap (hundreds of && uses, all bool-disciplined) survives. Gate pin: logical-operand-bool-nix-parity |
| `r2-phase-d-strict-default` | `smoke` | R2 PHASE D LANDED (owner doctrine 2026-07-07 — 'two languages, no blending': pnix and Clojure are separate languages; interop/meta-circular bridge values and evidence, never semantics; host characteristics leaking into the guest are DEFECTS, not dialect choices or decision problems): strict Nix typing is pnix's ONLY semantics now. The former lenient default (truthy if/assert/!, string+non-string '+', lenient string builtins) was a Clojure-truthiness HOST LEAK — removed, not 'flipped as a mode': evaluator *strict* defaults true (the var survives only as the audit lane's mechanism), the LOWERING lane gained require-bool guards on lowered if/assert/! plus a both-strings-only plus (its Clojure `if`/`str` were the same leak, live until this slice), and the px lane already held. Flip measurement BEFORE landing: 278 corpus+runtime sources, strict violations 0, strict-gate 269/269 ok — nothing real broke. eval-source-strict stays as an explicit alias; NO lenient escape hatch exists. Pins updated honestly: Phase A audit now asserts record+hold one-to-one, Phase C's default-lenient tail now asserts default-holds, D3's tryEval boundary block asserts default==strict==held-uncaught. Gate pin: strict-semantics-default-across-lanes (type errors hold on EVERY lane; well-typed forms stay :all-lanes-agree) |
| `host-leak-audit-guest-surface` | `smoke` | HOST-LEAK AUDIT LANDED (completes the owner's 'two languages, no blending' doctrine, 2026-07-07): the guest builtins surface was mechanically diffed against real Nix 2.34.7 (builtins.attrNames builtins, oracle-captured). VERDICT: 75 extra names are ALL guest EXTENSIONS — nixpkgs-lib flavored helpers, math, operator mirrors, list/attr conveniences, pnix-specific (pnixMounts) — they accept MORE programs but change the meaning of NO valid Nix program; zero remaining host leaks after R2 Phase D (the truthiness/coercion class was the leak, removed). 33 real-Nix names we lack = the impurity/store boundary (fetchers, currentTime, toFile, filterSource, nixPath, flake refs — conformance Phase D territory) + pure frontiers (toXML, fromTOML, hashString/hashFile) + the self-referential builtins attrset entries (builtins.import/true/false/null/builtins — small fidelity gap, filed). The whole inventory is PINNED in resources/pnix_clj/guest_surface.edn with the captured oracle list, and a drift gate makes any new extension a conscious registry update, never drift. Gate pin: guest-surface-extension-registry-drift |
| `f8-weval-ir-pe` | `weval` | F8 LANDED (bounded spike per docs/REMAINING_DECISION.md B): weval-shaped IR-level 1st Futamura on an interpreter BODY (pnix-clj.weval) — the pnix arith/bool/let/if core compiles (from the REAL parser AST) to a register IR; an IR interpreter with the classic fetch+case-dispatch pc loop is then SPECIALIZED per static program: every reachable pc becomes one residual block (pc-as-specialization-context — the researched pitfall is constant-prop collapse at the dispatch merge; the fix, MEMOIZED per-pc blocks so join points are SHARED not exponentially unrolled, is construction-checked block-count<=instr-count). Residual = STATIC Clojure form, no deopt, no instruction vector/pc/case (dispatch-free-by-construction walk-checked); dynamic-value semantics stay behind HAND-PLACED boundary fns shared by interpreter AND residual (weval intrinsics / @TruffleBoundary discipline — no automatic heuristics). Differential 4-way on a 12-source corpus with per-source input assignments: residual == IR-interpreter == real pnix evaluator (let-bound inputs, bool-proof grounding pattern) == clj-meta BYTECODE lane (eval-lowered, 12/12) — the architectural claim 'clj-meta hosts IR-level PE' is the bytecode-lane agreement. Dispatch metric: 211 interpreted fetch/dispatch steps vs 0 residual. Outside-fragment sources (lambda/attrset/list/recursive let) refuse honestly; div-by-zero holds identically on all lanes. Labels kept: correctness=construction-argument+differential-tests, performance=HEURISTIC dispatch-count (never wall-clock; weval's own ceiling ~2.17x, JIT 3.86x beyond). Gate pin: weval-ir-pe-dispatch-elimination |
| `b2-pnix-meta-loader` | — | B2 (monorepo shared-core) LANDED · pnix-clj loads the EXTERNAL common-.px core (~/pnix/pnix-meta) and runs it — pnix-clj.pnix-meta: load-corpus slurps an external root's .px into the in-memory target->source map (keyed by the ./relative import form the resolver matches) and eval-meta reuses the existing *import-modules* seam (NO eval-semantics change = non-regression). conformance-report runs every corpus/**.px with a sibling .expected.json and compares the PROVISIONAL canonical form (sorted-key json/write-json; the finalized cross-host canonical result is B1, owner-gated). The host OWNS loading; portable semantics are LOADED not copied (anti-cram, responsibility matrix). First proof: ./stdlib/bool.px loaded from the external root, exports called (xor/implies/and), and corpus bool-01 -> [false,false,true,true,true] matching the pin THREE ways (nix-instantiate oracle == pnix-clj == expected.json). The reference-host third of the eventual tri-host gate (B5). Alias :pnix-meta-conformance. Skips cleanly when the sibling ~/pnix/pnix-meta is absent (standalone checkout). Doctrine refs: ~/pnix/project-wiki/{CONSTITUTION,maps/shared-blockers-map (B2)}.md |
| `examples` | — | examples/ — plain-Clojure-limit vs pnix-clj-way pairs (01 sandbox, 03 futamura, 11 self-hosting, 12 cache) |
| `flake` | — | flake.nix — installable toolchain (nix run .#gate/.#tower/.#examples), from-source apps + devShell |
| `wiki` | — | docs/WIKI.md — this registry, machine-generated + gate-checked (grep-before-you-build) |
| `spine-canonical-cas` | `cas` | checklist §3 [BUILD 1st] LANDED (pnix-clj.cas): α-CANONICAL (§3b done — de-Bruijn lambda/let w/ correct shadowing, attrset labels + free vars kept) + structural canonicalization (order-independent attrset/let sort) + content-addressed term store; hash = PROPOSE filter CONFIRMED by exact structural equality (collision caught, not trusted); pure-EDN guard. Follow-up §3b = alpha-canonical (de-Bruijn w/ correct shadowing); §3c = open-term summary. Original: canonicalize-then-hash on a term GRAPH: order-independent attrset/let, letrec via bisimulation-collapse maximal sharing (Grabmayer-Rochel ICFP'14), positional binders + dependency-by-hash (Unison), alpha-aware hash (Maziarz PLDI'21). Hash-hit = PROPOSE only -> confirm by graph-isomorphism/alpha. Open-term summary via context-sensitive hashing (Blaauwbroek'24, NOT raw de-Bruijn Merkle = UNSOUND for open subterms). On main (cas.clj/term.clj) |
| `spine-cas-alpha` | `cas` | checklist §3b · ALPHA-canonicalization (de-Bruijn binders w/ CORRECT shadowing for lambda/let/rec-attrset) so alpha-renamed terms dedup; whole closed terms only (open-subterm de-Bruijn-Merkle is UNSOUND, Blaauwbroek'24). Upgrades cas v1 from structural to alpha canonical. |
| `spine-cas-summary` | `search` | checklist §3c · open-term structural summary (anonymous skeleton + free-var summary + structural distance) via context-sensitive hashing for §17 similarity search |
| `spine-event-log` | `store` | checklist §5 [BUILD 2nd] LANDED (pnix-clj.store): append-only VERIFYING trace (hashes+pure-EDN, values stay in §3/cached-eval); TAMPER-EVIDENT hash-chain (verify-chain); event hash/seq/index-by-kind/hash/field; pointer-movement-as-event; hermeticity CAS guard rejects Date/fn/host-object contamination. Orig: append-only EVENT log = VERIFYING trace (Build Systems a la Carte, ICFP'18): hashes only, scheduler ORTHOGONAL to rebuilder, VALUES stay in cached-eval/§3. CAS guard at append! rejects hermeticity contamination (build-IDs/timestamps/host-varying/source-tree writes — Bazel). On main (store.clj/evidence.clj) |
| `spine-snapshot-resolve` | `snapshot` | checklist §8 [BUILD 4th] · snapshot :id = hash(evaluator+symbol-version + §10/§13.1 reflection snapshots); runtime-match gate FAIL-CLOSED; resolve-term under snapshot. Content-addressed skip sound ONLY under determinism (Frankenbuild, Bazel CAS+command-history). On main (resolve.clj) |
| `spine-purity-events` | `purity` | checklist §9 [BUILD 5th] · determinism as EVENTS caught at replay/fork, pinned to FIRST-divergent event (Nakajima'26) = the §15 witness anchor; by ACTUAL re-run+diff (fold is NOT auto byte-identical; effects RECORDED not assumed); mutation isolation; threaded stress; payload date/timestamp scan. determinism.clj partial; full on main (purity.clj) |
| `spine-search` | `search` | checklist §17 [BUILD 6th] · search = content-address + event index + structural similarity (open-term skeleton distance from §3c). On main (search.clj) |
| `reflect-namespace-var` | `reflect` | checklist §10 [BUILD 3rd] · deterministic ns/var/meta/dynamic-binding SNAPSHOTS + diff + witness (sorted, no identity) — the host-varying inputs §8 must pin. Absent both lanes |
| `reflect-classpath-jvm` | `reflect` | checklist §13.1 [BUILD 3rd] · classpath + JVM-version snapshot/hash — pins host-varying binaries (Bazel hermeticity). Absent on feat (clj-meta jarproof has jar hashing) |
| `mirror-drift-chain` | `mirror-chain` | checklist §6.6-6.7 [BUILD 7th] · mirror DRIFT events + repeated-run chain convergence stability, recorded as §5 events. Cross-mirror per-run verdict exists (mirror.clj) |
| `generator-obs-eq` | `generate` | candidate GENERATOR #1 (pnix-clj.generate): observational-equivalence-reduced bottom-up enumerative synthesis (Escher CAV'13). value-vector via core/eval-source dedups behavioral classes (919->111 on a sample); a value-vector match = HEURISTIC PROPOSE (observational, not proven); synthesize-and-propose feeds matches to self-improve as HELD proposals. Decision + sequencing in docs/GENERATOR_DECISION.md (from /deep-research, 16 claims 3-0). Closes the self-* loop with a real generator |
| `generator-cegis` | `cegis` | candidate GENERATOR #2 LANDED (pnix-clj.cegis): CEGIS refinement (Smyth/Burst loop) -- the verifier DRIVES the generator: probe the enumerated candidate against a REFERENCE over deterministic inputs; the first divergence becomes a counterexample appended to the examples; retry. On survival, arith-proof (canonical polynomial) UPGRADES the verdict to :proven, else honestly :heuristic-no-counterexample. Iterations recorded as §5 :cegis/iteration events; converged candidates fed to self-improve as HELD proposals |
| `generator-canonical-prune` | `generate` | candidate GENERATOR #3 LANDED (in pnix-clj.generate/synthesize): Knuth-Bendix-style canonical pre-pruning -- a candidate whose canonical POLYNOMIAL (arith-proof) was already seen is a PROVEN duplicate, skipped before evaluation; non-arith falls back to observational dedup. SOUND (match set provably unchanged) + measured 88% pruned (919 enumerated -> 111 evaluated), beating the research ~80% estimate |
| `self-improve-loop` | `self-improve` | self-* loop BODY (pnix-clj.self-improve): evaluate a batch of candidate self-modifications -- witness each (run-witnessed) + gate each (self-mod-gate) + rank best-first + record the round as §5 events. GENERATION-AGNOSTIC (candidates are input, not claimed-invented); every proposal stays HELD under the default policy = a review queue for the owner, never auto-applied. The honest bounded shape of the north-star recursive self-* loop |
| `self-mod-gate` | `self-mod-gate` | checklist §14.3 · self-modification gate (pnix-clj.self-mod-gate): the constitution's NO-AUTO-PROMOTION invariant as a runtime gate -- a self-mod proposal earns a §15 witness via run-witnessed, but an :admitted witness is HELD by default (owner must sign off); only an explicit :owner-authorized policy promotes; unknown policy fails closed to :held; decisions recorded as §5 :self-mod/* events |
| `spine-replay` | `replay` | REPLAY/AUDIT (pnix-clj.replay): independently re-verify a persisted §15 witness in a fresh run -- load witness+source from disk, re-run witnessed, compare term-hash(§3)+result-hash(§9)+snapshot(§8). :reproduced iff identical, else :diverged pinned to the changed field. The §8/§9 determinism guarantee checked ACROSS TIME/PROCESS, entirely from durable evidence |
| `spine-persist` | `persist` | DURABLE persistence (pnix-clj.persist): content-addressed on-disk backing for §3 terms (<dir>/terms/<hash>.edn) + §5 events (append-only events.log), reverified on load (chain intact + term hashes back to its key). Unison/Nix-store shape. Explicit side-effect op, never part of pure eval; evidence trail survives across runs |
| `spine-integration` | `witnessed-run` | SPINE INTEGRATION (pnix-clj.witnessed-run): one run ties the spine to the pillars -- §3 term-hash keys, §8 snapshot pins, tower collapse (§6) + mirror chain (§6.6-6.7) + determinism (§9) recorded as §5 events, Futamura residual content-addressed by (term-hash+snapshot-id+bytecode-hash), §15 witness admitted iff collapsed+converged+deterministic |
| `spine-witness-capstone` | `witness` | checklist §15 [BUILD 8th, CAPSTONE] · explicit witness schema (:witness/id, in/out/term/result hash, runtime/compiler/evaluator version, snapshot id, stage, status, evidence events) + admission lattice (held/candidate/admitted/rejected/evidence/failed/ok), in-toto/SLSA-shaped. Integrates all: Futamura residual bytecode content-addressed by (term-hash+snapshot-id); cross-mirror verdicts as §5 events feed §15 |
| `host-parser-let-if-rhs` | `grammar-fuzzer` | host-parser-let-if-rhs FIXED — the host parser now rejects an unparenthesized let/if/with/assert as an operator OPERAND (guard at every operand-after-operator position: binary levels, -> RHS, !/- unary), exactly where real Nix rejects (oracle-confirmed, all 4 keywords). Leftmost/parenthesized/body positions unchanged (same tree as Nix expr_function > expr_op). Found by the property fuzzer; .px parser always agreed. Follow-up landed same day: app-arg and list-element positions guarded too (all Nix-rejected positions for these 4 keywords now covered) |
| `px-scope-laziness` | `mirror-pair` | px scopedImport scope laziness LIFTED — the .px lane now only WHNF-forces the scope; values cross the px→host boundary as-is and the host wraps each slot in its own call-by-need thunk (px_runtime/bridge-px-scope-value: iterative host-thunk + px-Thunk-record unwrap at force time, children re-bridged lazily). An erroring UNUSED scope attr never evaluates — real Nix laziness on all 4 lanes (corpus: scoped-import-lazy-unused-scope-attr, scoped-import-lazy-nested-unused-attr) |
| `splice-quote-leniency` | `smoke` | `"`-in-`${}` splice leniency · REJECTED as a work item (2026-07-08 deep-research, 3-0, docs/REMAINING_DECISION.md): the premise was FALSE — real Nix does NOT reject double-quoted strings inside `${…}` (the splice opens a full expression context; nixpkgs relies on nested strings pervasively; `"${"foo"}"`→foo etc. all accepted on the repo oracle). D7's balanced-scanner (px-lane-string-nesting-parity / string-nesting-tokenizer-nix-parity) is CORRECT Nix, not leniency — KEEP it; removing it would REDUCE conformance and force a needless corpus migration. Only true residue = `\"`-ESCAPED quotes inside splices, a micro-quirk not worth migrating either way. No corpus change; no code change — decision recorded, pinned by the D7 gates that already stand |

## Roadmap — in flight

_(none)_

## Roadmap — planned

| id | capability | title |
|---|---|---|
| `d1c-deep-nontail-eval` | — | D1c · DEFERRED as conformance; the PILLAR route landed as M7 (the derived abstract machine evaluates the exact D1c shapes — plus-chain-100k, nested-list-100k — in constant stack on its fragment, while the tree-walk lane keeps the graceful bound). Verdict (2026-07-08 deep-research, docs/REMAINING_DECISION.md): the graceful structured :stack-overflow bound ALREADY IS the Nix-parity answer — real Nix gives NO deep non-tail guarantee either (native C++ stack + max-call-depth counting FUNCTION calls only, not data nesting; open issue #9627), so it segfaults machine-dependently on our exact nested-list/left-spine shapes → conformance value ≈0. Touch ONLY as PILLAR work: derive the abstract machine via the functional correspondence (closure-conv + CPS + defunctionalization → CEK/Krivine; Ager-Biernacki-Danvy-Midtgaard PPDP'03), a metacircular/projection artifact. ★REFUTED justifications (0-3): trampoline (clojure.core/trampoline) and store-allocated continuations (arXiv 1007.4446) — do NOT build on those; call-by-need needs the Krivine+memoizing-store (CESK) refinement. Was: oracle-confirmed 40k-ok/100k-SOE cliff, plus-chain-100k held (Nix timed out 60s) |
| `f7b-self-applicable-specializer` | — | F7b · HELD (owner sign-off; deep-research 07-07 AND RE-CONFIRMED 07-08 verdict: OPEN RESEARCH — no call-by-need-native self-applicable PE exists; Similix specializes non-strict interpreters but the specializer is STRICT and laziness is object-level suspension = call-by-name w/o sharing) — no self-applicable PE written in a call-by-need language exists in the literature (all classical: mix/Scheme0/lambda-mix/Logimix/Similix = strict; nearest = Mogensen normal-order λ, no sharing); the PROVEN lazy route is our existing strict-host specializer (Bondorf'90/Jørgensen POPL'92); even success = only a PARTIAL correctness test (Glück §5.2). If attempted: JGS §7.4 11-step recipe (bare-bones core → self-interpreter → annotate → Clojure spec first → pnix LAST), go/no-go = Jones-optimality. docs/REMAINING_DECISION.md |
| `nix-conformance-phase-d` | — | Nix conformance Phase D — DEFERRED (2026-07-08 deep-research verdict, docs/REMAINING_DECISION.md): conformance MATERIAL, build only when a pillar needs it, and then only the PURE subset. Mirror Tvix's architecture: swappable EvalIO trait (default DummyIO) + builder_pure/builder_impure + pure_builtins module vs. impure module (just 7: getEnv/hashFile/pathExists/readDir/readFile/readFileType/currentTime). Pure/hermetic subset = hashString, fromTOML, toXML, toFile→content-addressed path, deterministic path realization (no daemon/network); fetchers/currentTime/currentSystem/flake-refs/findFile are genuinely impure → behind the seam, only simulatable. Even a full Tvix keeps the store OUT of the evaluator (tvix-store/castore). (3-0) |

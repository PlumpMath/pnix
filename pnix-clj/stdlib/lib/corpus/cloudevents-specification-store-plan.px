# stdlib/lib/corpus/cloudevents-specification-store-plan.px — CloudEvents specification metadata.
# redb `corpus_source_records`에 pnix attrset source(JSON 아님)로 append한다.
# event payloads/endpoints/credentials/invocation/graph wiring 없음.
let
  B = builtins;
  genPath = ./cloudevents-specification.generated.px;
  seed = if B.pathExists genPath then import genPath else {
    schema = "api.cloudevents.specification.v1";
    source = { name = "CloudEvents official specification documents and format schemas"; license = "Apache-2.0"; scope = "empty seed fallback"; };
    summary = { source_file_count = 0; table_row_count = 0; token_count = 0; json_property_count = 0; };
    source_files = [ ]; table_rows = [ ]; tokens = [ ]; json_properties = [ ]; avro_fields = [ ]; proto_fields = [ ];
  };
  ser = import ../nl/pnix-value-literal.px;
  valueSrc = ser.toPnixSource seed;
  contentHash = B.hashString "sha256" valueSrc;
  key = seed.schema + "/" + contentHash;
in {
  mkPlan = args: {
    plan = {
      plan_id = "cloudevents-specification-load-v0";
      source_lens = "stdlib/lib/corpus/cloudevents-specification-store-plan.px";
      source_verdict = "materialize-corpus-source";
      operations = [ { op_kind = "append"; table = "corpus_source_records"; key = key; value_bytes = valueSrc; } ];
      receipt_owner = "api.cloudevents.specification.v0";
      plan_built_at_ms = args.built_at_ms or 0;
    };
    schema = seed.schema;
    content_hash = contentHash;
    value_bytes_len = B.stringLength valueSrc;
  };
  mkQuery = args: {
    plan = {
      plan_id = "cloudevents-specification-query-v0";
      source_lens = "stdlib/lib/corpus/cloudevents-specification-store-plan.px";
      source_verdict = "query-corpus-source";
      operations = [ { op_kind = "scan-prefix"; table = "corpus_source_records"; key = ""; key_prefix = seed.schema + "/"; } ];
      receipt_owner = "api.cloudevents.specification.v0";
      plan_built_at_ms = args.built_at_ms or 0;
    };
    expected_key = key;
    expected_hash = contentHash;
  };
}

# EPA RadNet catalog metadata -> repo-local redb corpus_source_records.
# Stores pnix attrset source, not JSON. No measurements/dose/safety decisions/graph wiring.
let
  B = builtins;
  genPath = ./epa-radnet-catalog.generated.px;
  seed = if B.pathExists genPath then import genPath else {
    schema = "science.epa_radnet.catalog.v1";
    source = "EPA RadNet catalog metadata";
    license = "US-PD / US federal public information";
    summary = { pages = 0; links = 0; files = 0; };
    policy = "official EPA RadNet catalog page metadata only";
    manifest = { };
    files = [ ]; pages = [ ];
  };
  ser = import ../nl/pnix-value-literal.px;
  valueSrc = ser.toPnixSource seed;
  contentHash = B.hashString "sha256" valueSrc;
  key = seed.schema + "/" + contentHash;
in {
  mkPlan = args: {
    plan = {
      plan_id = "epa-radnet-catalog-load-v0";
      source_lens = "stdlib/lib/corpus/epa-radnet-catalog-store-plan.px";
      source_verdict = "materialize-corpus-source";
      operations = [ { op_kind = "append"; table = "corpus_source_records"; key = key; value_bytes = valueSrc; } ];
      receipt_owner = "science.epa_radnet.catalog.v0";
      plan_built_at_ms = args.built_at_ms or 0;
    };
    schema = seed.schema;
    content_hash = contentHash;
    value_bytes_len = B.stringLength valueSrc;
  };
  mkQuery = args: {
    plan = {
      plan_id = "epa-radnet-catalog-query-v0";
      source_lens = "stdlib/lib/corpus/epa-radnet-catalog-store-plan.px";
      source_verdict = "query-corpus-source";
      operations = [ { op_kind = "scan-prefix"; table = "corpus_source_records"; key = ""; key_prefix = seed.schema + "/"; } ];
      receipt_owner = "science.epa_radnet.catalog.v0";
      plan_built_at_ms = args.built_at_ms or 0;
    };
    expected_key = key;
    expected_hash = contentHash;
  };
}

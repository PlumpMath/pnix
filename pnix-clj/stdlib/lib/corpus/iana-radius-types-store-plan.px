# IANA RADIUS Types redb store-plan. 적재까지만, graph/mirror/math 연결 없음.
let
  B = builtins;
  genPath = ./iana-radius-types.generated.px;
  seed = if B.pathExists genPath then import genPath else { schema = "registry.iana.radius_types.v1"; source = { project = "IANA RADIUS Types"; }; registries = [ ]; registry_count = 0; record_count = 0; };
  ser = import ../nl/pnix-value-literal.px;
  valueSrc = ser.toPnixSource seed;
  contentHash = B.hashString "sha256" valueSrc;
  key = seed.schema + "/" + contentHash;
in {
  mkPlan = args: {
    plan = {
      plan_id = "registry-iana-radius-types-load-v0";
      source_lens = "stdlib/lib/corpus/iana-radius-types-store-plan.px";
      source_verdict = "materialize-registry-iana-radius-types";
      operations = [ { op_kind = "append"; table = "corpus_source_records"; key = key; value_bytes = valueSrc; } ];
      receipt_owner = "registry.iana.radius_types.v0";
      plan_built_at_ms = args.built_at_ms or 0;
    };
    schema = seed.schema;
    content_hash = contentHash;
    value_bytes_len = B.stringLength valueSrc;
  };
  mkQuery = args: {
    plan = {
      plan_id = "registry-iana-radius-types-query-v0";
      source_lens = "stdlib/lib/corpus/iana-radius-types-store-plan.px";
      source_verdict = "query-registry-iana-radius-types";
      operations = [ { op_kind = "scan-prefix"; table = "corpus_source_records"; key = ""; key_prefix = seed.schema + "/"; } ];
      receipt_owner = "registry.iana.radius_types.v0";
      plan_built_at_ms = args.built_at_ms or 0;
    };
    expected_key = key;
    expected_hash = contentHash;
  };
}

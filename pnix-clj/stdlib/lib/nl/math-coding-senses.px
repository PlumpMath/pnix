# stdlib.nl.math-coding-senses
#
# **math/code 용어 정의 (.px 데이터, project-owned): front-door 정의질문 route 용.**
#
# 운영자(2026-06-15): nl-definition-query 가 답하려면 senses 가 필요한데, 순수 평가기는
# readFile 이 없어 corpus JSON 을 .px 에서 못 읽는다. 그래서 project-owned 정의를 .px
# 데이터로 두어 front-door 가 import → 정의질문 즉시 답(host 배선 없이 live).
#
# 출처: corpus/dictionary/ko-math-coding.senses.json (pnix 저작, license-deny 0매칭,
# 게이트 통과). 이건 bridge — 장기적으로 host 가 corpus senses 를 push 하면 그걸 쓴다.
# anti-transcript: 정의는 pnix 저작(외부 verbatim 아님).
{
  schema = "pnixc-meta.math-coding-senses.v0";
  source_id = "pnix-project-seed-math-coding-ko-v0";
  license_ref = "project-owned";
  rows = [
    { domain = "math"; lemma = "소수"; pos = "명사"; definition_surface = "1과 자기 자신만을 약수로 가지는, 1보다 큰 자연수."; }
    { domain = "math"; lemma = "약수"; pos = "명사"; definition_surface = "어떤 수를 나누어떨어지게 하는 수."; }
    { domain = "math"; lemma = "배수"; pos = "명사"; definition_surface = "어떤 수를 정수배 하여 얻는 수."; }
    { domain = "math"; lemma = "최대공약수"; pos = "명사"; definition_surface = "두 수 이상의 공통된 약수 가운데 가장 큰 수."; }
    { domain = "math"; lemma = "최소공배수"; pos = "명사"; definition_surface = "두 수 이상의 공통된 배수 가운데 가장 작은 수."; }
    { domain = "math"; lemma = "분수"; pos = "명사"; definition_surface = "두 정수의 비로 나타낸 수로, 분자를 분모로 나눈 값을 뜻한다."; }
    { domain = "math"; lemma = "정수"; pos = "명사"; definition_surface = "0과 자연수, 그리고 자연수에 음의 부호를 붙인 수를 통틀어 이르는 수."; }
    { domain = "math"; lemma = "유리수"; pos = "명사"; definition_surface = "두 정수의 비로 나타낼 수 있는 수."; }
    { domain = "math"; lemma = "방정식"; pos = "명사"; definition_surface = "미지수의 값에 따라 참이 되기도 하고 거짓이 되기도 하는 등식."; }
    { domain = "math"; lemma = "일차방정식"; pos = "명사"; definition_surface = "미지수의 최고 차수가 1인 방정식."; }
    { domain = "math"; lemma = "이차방정식"; pos = "명사"; definition_surface = "미지수의 최고 차수가 2인 방정식."; }
    { domain = "math"; lemma = "판별식"; pos = "명사"; definition_surface = "이차방정식의 근의 종류를 가려내는 식으로, 계수로부터 b의 제곱에서 4ac를 뺀 값을 말한다."; }
    { domain = "math"; lemma = "소인수분해"; pos = "명사"; definition_surface = "자연수를 소수인 인수들의 곱으로 나타내는 일."; }
    { domain = "math"; lemma = "함수"; pos = "명사"; definition_surface = "한 집합의 각 원소에 다른 집합의 원소를 하나씩 대응시키는 관계."; }
    { domain = "math"; lemma = "미분"; pos = "명사"; definition_surface = "함수의 순간 변화율을 구하는 연산."; }
    { domain = "coding"; lemma = "변수"; pos = "명사"; definition_surface = "값을 저장하고 이름으로 가리키는 기억 공간."; }
    { domain = "coding"; lemma = "반복문"; pos = "명사"; definition_surface = "같은 동작을 조건이 만족하는 동안 되풀이하는 제어 구조."; }
    { domain = "coding"; lemma = "조건문"; pos = "명사"; definition_surface = "조건의 참과 거짓에 따라 실행 흐름을 가르는 제어 구조."; }
    { domain = "coding"; lemma = "배열"; pos = "명사"; definition_surface = "같은 종류의 값을 순서대로 모아 놓은 자료 구조."; }
    { domain = "coding"; lemma = "재귀"; pos = "명사"; definition_surface = "함수가 자기 자신을 불러서 문제를 푸는 방법."; }
    { domain = "coding"; lemma = "리팩터링"; pos = "명사"; definition_surface = "겉으로 드러나는 동작은 그대로 두고 코드의 내부 구조를 개선하는 일."; }
    { domain = "coding"; lemma = "컴파일"; pos = "명사"; definition_surface = "소스 코드를 기계가 실행할 수 있는 형태로 번역하는 일."; }
    { domain = "coding"; lemma = "디버깅"; pos = "명사"; definition_surface = "프로그램의 결함을 찾아 고치는 일."; }
    { domain = "math"; lemma = "자연수"; pos = "명사"; definition_surface = "1부터 시작하여 하나씩 더해 얻는 수."; }
    { domain = "math"; lemma = "등식"; pos = "명사"; definition_surface = "두 식이나 수가 서로 같음을 등호로 나타낸 식."; }
    { domain = "math"; lemma = "평균"; pos = "명사"; definition_surface = "여러 수의 합을 그 개수로 나눈 값."; }
    { domain = "math"; lemma = "삼각형"; pos = "명사"; definition_surface = "세 개의 선분으로 둘러싸인 평면 도형."; }
    { domain = "coding"; lemma = "알고리즘"; pos = "명사"; definition_surface = "문제를 풀기 위한 단계적인 절차나 방법."; }
    { domain = "coding"; lemma = "자료구조"; pos = "명사"; definition_surface = "자료를 효율적으로 저장하고 다루기 위한 구조."; }
    { domain = "coding"; lemma = "객체"; pos = "명사"; definition_surface = "데이터와 그 데이터를 다루는 동작을 하나로 묶은 단위."; }
  ];
}
